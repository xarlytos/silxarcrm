# GroqAgent Dependencies
# Add these to requirements.txt or pyproject.toml

# HTTP client for Groq API calls
httpx>=0.24.0,<1.0.0

# Optional: Groq official SDK (alternative to httpx)
# groq>=0.4.0

# Testing dependencies (already likely present)
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-mock>=3.10.0
unittest-mock>=1.5.0

# Observability (optional, for latency tracking)
opentelemetry-api>=1.18.0
opentelemetry-sdk>=1.18.0
opentelemetry-exporter-prometheus>=0.39b0
opentelemetry-exporter-jaeger>=1.18.0

# Type hints
typing-extensions>=4.0.0

# Logging & structured output
python-json-logger>=2.0.0
